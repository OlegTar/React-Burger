import { FC } from 'react';
import styles from './orders-summary.module.scss';

export const OrdersSummary: FC = () => {
	return (
		<section className={`ml-15 mt-25 ${styles['orders-summary']}`}>
			OrdersSummary
		</section>
	);
};
